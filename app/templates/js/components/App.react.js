var React = require("react");
var Header = require("./Header.react");

var App = React.createClass({
    render: function() {
        return (
            <Header />
        );
    }
});

module.exports = App;